language: python
#python:
#  - 3.6
branches:
  only:
    - ci_test

build:
  ci:
    - sudo apt-get install bluez-test-tools
    - btvirt -l2 &
    - btmgmt info
    - python3 setup.py clean build sdist publish

